# AI Voice Caller — Python Dependencies
# Install: pip install -r requirements.txt

# ── Core ──────────────────────────────────────────────────────────────────────
requests>=2.28
flask>=3.0

# ── V2 State Machine DB ───────────────────────────────────────────────────────
# ORM + schema validation for accounts_v2, call_attempts, agents, referrals
SQLAlchemy>=2.0
pydantic>=2.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=9.0

# ── Salesforce sync ───────────────────────────────────────────────────────────
# simple-salesforce>=1.12

# ── Research cache L2 (BigQuery — sled_intelligence.research_cache) ──────────
google-cloud-bigquery>=3.0

# ── Firestore (legacy campaign_runner.py) — not needed for V2 ─────────────────
# google-cloud-firestore>=2.0
# google-cloud-secret-manager>=2.0

# ── SignalWire Python SDK (agents/ dir — not required for inline SWML calls) ──
# signalwire-agents>=0.1
